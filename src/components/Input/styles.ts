import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    backgroundColor: "#FFF",
    paddingHorizontal: 16,
    height: 48,
    width: "100%",
    borderRadius: 8,
    borderWidth: 1,
    borderColor: "#c6c5cb"
  },
});
